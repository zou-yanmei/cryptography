# Lab1：穷举法破译凯撒密码

## 实验背景

凯撒密码是一种最古老的替换加密方式，通过将字母表中的每个字母向后移动固定位数 $k$（密钥）来加密明文。

例如，当 $k=3$ 时：

```
明文：HELLO
密文：KHOOR
```

由于密钥 $k$ 的取值范围只有 1~25，因此可以用**穷举法**（暴力破解）枚举所有可能的密钥，逐一尝试解密，直到找到有意义的明文。

---

## 实验任务

以下是一段凯撒密码加密后的密文：

```
NUFECMWBYUJMBIQGYNBYWIXY
```

请编写程序，使用穷举法枚举所有可能的密钥（1~25），输出每种密钥对应的解密结果，并指出正确的密钥和明文。

---

## 实验要求

1. 使用任意编程语言实现（推荐 Python）
2. 程序需输出所有 25 种可能的解密结果，格式如下：

```
k=1  : MTEDBLAVXTILAHOEXMAXVHWX
k=2  : LSDCAKZUWSHZAGNWDLZWUGVW
...
...
```

3. 在代码注释或实验报告中说明：
   - 正确的密钥 $k$ 是多少
   - 解密后的明文是什么
   - 你是如何判断哪个结果是正确明文的

---

## 提交要求

在自己的文件夹下新建 `Lab1/` 目录，提交以下文件：

```
学号姓名/
└── Lab1/
    ├── caesar.py       # 源代码
    └── report.md       # 简要说明（密钥、明文、判断依据）
```

---

## 截止时间

2026-3-17 ， 届时关于Lab1的PR请求将不会被合并。

---

## 参考资料

- [凯撒密码 - 百度百科](https://baike.baidu.com/item/%E6%81%BA%E6%92%92%E5%AF%86%E7%A0%81/4905284)

